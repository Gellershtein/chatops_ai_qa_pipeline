# Telegram Bot Token - Get this from BotFather on Telegram
TELEGRAM_BOT_TOKEN="YOUR_TELEGRAM_BOT_TOKEN"

# MinIO Configuration
MINIO_ENDPOINT="localhost:9000"
MINIO_ACCESS_KEY="minioadmin"
MINIO_SECRET_KEY="minioadmin"
MINIO_BUCKET="qa-pipeline"
# Set to "true" for HTTPS, "false" for HTTP (MinIO local usually uses HTTP)
MINIO_SECURE="false" 

# Google Gemini API Key - Get this from Google AI Studio
GEMINI_API_KEY="YOUR_GEMINI_API_KEY"
GEMINI_MODEL_NAME="gemini-pro"
GEMINI_TEMPERATURE="0.7"

# LLM Configuration
LLM_PROVIDER="cloud" # Options: "cloud", "local"
CLOUD_MODEL_NAME="gemini-pro"
LOCAL_MODEL_NAME="codegemma:7b" # Example local model name (from user's curl)
LOCAL_LLM_ENDPOINT="http://host.docker.internal:11434" # Example for local LLM (e.g., Ollama)