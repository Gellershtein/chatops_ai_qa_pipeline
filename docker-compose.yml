version: "3.9"

services:
  minio:
    image: minio/minio
    ports:
      - "9000:9000"
      - "9001:9001"
    env_file:
      - .env
    command: server /data --console-address ":9001"
    networks:
      - qa-net

  app:
    build: .
    depends_on:
      - minio
    env_file:
      - .env
    volumes:
      - .:/app
    command: >
      sh -c "python storage/minio_setup.py && python -m bot.main"
    networks:
      - qa-net

networks:
  qa-net:
    driver: bridge